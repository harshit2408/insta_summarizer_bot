# ── Instagram Scraper ────────────────────────────────────────────────────────
yt-dlp>=2024.3.10
requests>=2.31.0

# ── Content extraction (Content Extractor Lambda — Docker image) ─────────────
# Uncomment if running the extractor locally (needs ffmpeg on PATH).
# faster-whisper>=1.0.0
# easyocr>=1.7.0
# opencv-python-headless>=4.8.0
# pillow>=10.1.0

# ── AI (AI Analyzer Lambda) ──────────────────────────────────────────────────
# The Lambda uses a stdlib-only urllib client; these are only needed for local
# experimentation or alternative client code.
# groq>=0.4.0

# ── Telegram Bot ─────────────────────────────────────────────────────────────
# python-telegram-bot>=20.7

# ── Google Docs / OAuth (Phase 2 Week 4) ────────────────────────────────────
# The Lambdas use urllib + boto3 only (no SDK) so cold starts stay <1 second.
# Install these locally to run the Google helpers in scripts or notebooks.
google-auth>=2.28.0
google-auth-oauthlib>=1.2.0
google-api-python-client>=2.118.0

# Token encryption for OAuth Handler + Google Docs Writer Lambdas. Replaces the
# former AWS KMS round trip with in-Lambda AES-256-GCM (key fetched from SSM).
# Must be bundled into both Lambda zips (it ships compiled extensions — install
# with --platform manylinux2014_x86_64 / matching the Lambda runtime).
cryptography>=42.0.0

# ── AWS SDK ──────────────────────────────────────────────────────────────────
boto3>=1.34.0

# ── Data validation & utilities ──────────────────────────────────────────────
pydantic>=2.5.0
python-dotenv>=1.0.0

# ── Testing ──────────────────────────────────────────────────────────────────
pytest>=7.4.0
pytest-cov>=4.1.0
